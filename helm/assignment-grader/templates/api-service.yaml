{{- if .Values.tags.api }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "assignment-grader.fullname" . }}-api
  labels:
    {{- include "assignment-grader.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assignment-grader.api.selectorLabels" . | nindent 4 }}
{{- end }}
